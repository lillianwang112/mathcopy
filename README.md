# mathcopy
A cross-browser extension that intercepts copy events on pages containing KaTeX or MathJax rendered math, replacing the visual gibberish in the clipboard with clean LaTeX wrapped in > `$...$` (inline) or `$$...$$` (display), while preserving all surrounding plain text verbatim.
